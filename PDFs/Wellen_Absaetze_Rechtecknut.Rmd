---
title: "Wellennachweis - umlaufende Rechtecknut"
author: "Quentin Huss, Nadine Schulz"
date: "`r format(Sys.time(), '%d.%m.%y, %X')`"
output: pdf_document
header-includes:
- \usepackage{booktabs}
- \usepackage{longtable}
- \usepackage{array}
- \usepackage{multirow}
- \usepackage{wrapfig}
- \usepackage{float}
- \usepackage{colortbl}
- \usepackage{pdflscape}
- \usepackage{tabu}
- \usepackage{threeparttable}
- \usepackage{threeparttablex}
- \usepackage[normalem]{ulem}
- \usepackage{makecell}
- \usepackage{xcolor}
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

```{r, echo=FALSE}

```

![](C:\\Users\\Nadine\\Documents\\Studium\\Studium\\1234567890\\ME_Wellen\\ME_Wellennachweis\\PDFs\\Rechtecknut.png)

# 0 gegebene Größen

## Geometrie an Stelle `r z_Koordinate` mm der `r Welle`  

\begin{center}
\begin{tabular}{|ll|}
Wellendurchmesser & $D = `r dw`mm$ \\
Tiefe der Nut & $t = `r t`mm$ \\
Radius & $r = `r r`mm$ \\
Breite der Nut & $b = `r b`mm$
\end{tabular}
\end{center}

\pagebreak


## Beanspruchung

Biegemittelspannung:

$$
\sigma_{bm} = 0 \dfrac{N}{mm^{2}}
$$ Biegeausschlagspannung:

$$
\sigma_{ba} = `r round(sigma_max, 3)` \dfrac{N}{mm^{2}}
$$

Torsionsmittelspannung:

$$
\tau_{tm} = 0 \dfrac{N}{mm^{2}}
$$

Torsionsausschlagspannung:

$$
\tau_{ta} = `r round(tau_max, 3)` \dfrac{N}{mm^{2}}
$$

# 1 Bauteilwechselfestigkeiten

## Kerbwirkungszahlen

$$\beta_{\sigma_b} = `r round(beta_sigma, 3)`$$

$$\beta_{\tau} = `r round(beta_tau, 3)`$$

## Gesamteinflussfaktoren

$$K_{\sigma ,b} = `r round(K_ges_sigma, 3)`$$

$$K_{\tau} = `r round(K_ges_tau, 3)`$$

## Bauteilwechselfestigkeit

$$\sigma_{bWK} = `r round(sigma_bWK, 3)` \dfrac{N}{mm^{2}}$$

$$\tau_{tWK} = `r round(tau_bWK, 3)` \dfrac{N}{mm^{2}}$$

# 2 Bauteilfließgrenzen

$$\sigma_{bFK} = `r round(sigma_bFK, 3)` \dfrac{N}{mm^{2}}$$

$$\tau_{tFK} = `r round(tau_tFK, 3)` \dfrac{N}{mm^{2}}$$

# 3 Gestaltfestigkeit

$$\sigma_{bADK} = `r round(sigma_bADK, 3)` \dfrac{n}{mm^{2}}$$

$$\tau_{tADK} = `r round(tau_tADK, 3)` \dfrac{N}{mm^{2}}$$

# 4 Sicherheiten

$$S_F = `r round(S_F, 3)`$$

$$S_D = `r round(S_D, 3)`$$
